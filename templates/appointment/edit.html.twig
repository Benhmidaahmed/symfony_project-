{% extends layout %}

{% block body %}
<div class="container">
     {# <h1>Modifier votre rendez-vous</h1> #}
    {{ include('appointment/_form.html.twig', {'button_label': 'Modifier'}) }}

    {# <a href="{{ path('app_appointment_index') }}#liste"class="btn btn-secondary ">
            <i class="fas fa-arrow-left"></i>Retour </a> #}

    {# {{ include('appointment/_delete_form.html.twig') }} #}
    </div>
{% endblock %}
