<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="google-site-verification" content="tn5WZbSsSZkhIhkuh_VI7JeY6eernyK3q4oiqBZVvdg" />
    <!-- ======= Styles ====== -->
    <link href="{{ asset('assets/img/favicon.ico')}}" rel="icon">
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
        <link rel="icon" type="image/x-icon" href="https://www.cabinethoujimolka.tn/favicon.ico">
   
    <link href="{{ asset('assets/admin/css/style.css') }}" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
 .table-container {
    width: 90%;  /* Ajuste la largeur */
    margin: 0 auto; /* Centre horizontalement */
    text-align: center;
    display: flex;
    justify-content: center;
}
/* Rendre le tableau plus large */
.table-container {
    max-width: 100%;
    width: 90%; /* Ajuste la largeur selon tes besoins */
    margin: auto; /* Centre le tableau */
}

/* Si la carte limite la largeur, élargis-la aussi */
.card {
    width: 100%;
}

/* Ajuster la largeur de la table elle-même */
.table {
    width: 100%;
}

.table-container table {
    width: 100%; /* La table prend toute la largeur du conteneur */
}

/* Ajustements pour les petits écrans */
@media (max-width: 991.98px) {
    .table-container {
        width: 100%; /* Prend toute la largeur sur les petits écrans */
    }
}

    .liste {
    text-align: center; /* Centre le texte */
    font-weight: 600; /* Épaisseur du texte */
    color: var(--blue); /* Applique la couleur */
    margin-left:30px;
}
.button{
    margin-left:90px;
    margin-bottom:10px;
}
/* .form-container{
    margin-left:400px;
    
} */
.cardHeader {
  display: flex;
  flex-direction: column; /* Permet d'empiler les éléments verticalement */
  align-items: flex-start; /* Aligne le texte à gauche */
}

.search {
  margin-top: 10px; /* Ajoute un espace entre le titre et l'input */
  width: 100%; /* Ajuste la largeur */
}
.serch-outline{
   margin-top:10px;
}
@media (max-width: 991.98px) {
       .liste {
    margin-left:10px;
}
.button{
    margin-left:0;
    margin-top:10px;
}
.form-container{
    margin-left:20px;
    
}
    /* .table-container {
    margin-left:60px;
} */
}
.details {
    display: flex;
    gap: 20px; /* Espace entre les deux div */
}

.recentOrders {
    flex: 2; /* Prend deux fois plus d'espace que recentCustomers */
    overflow-y: auto; /* Ajoute une barre de défilement si nécessaire */
    max-height: 600px; /* Hauteur maximale pour recentOrders */
}

.recentCustomers {
    flex: 1; /* Prend un espace normal */
    overflow-y: auto; /* Ajoute une barre de défilement si nécessaire */
    max-height: 600px; /* Hauteur maximale pour recentCustomers */
}
.btn-container {
    width: 100%; /* Prend toute la largeur de son parent (recentOrders) */
}

.btn-container .btn {
    width: 100%; /* Le bouton prend toute la largeur de son conteneur */
    text-align: center; /* Centrer le texte du bouton */
    padding: 10px; /* Ajouter un peu de padding pour un meilleur rendu */
    box-sizing: border-box; /* Inclure le padding dans la largeur totale */
}

@media screen and (max-width: 768px) {
    .details {
        flex-direction: column; /* Affiche les éléments en colonne */
    }

    .recentOrders,
    .recentCustomers {
        flex: none; /* Supprime la flexibilité */
        width: 100%; /* Prend toute la largeur */
        max-height: none; /* Supprime la hauteur max pour s'adapter au contenu */
    }
}
</style>


    <title>{% block title %}Cabinet d'Audiologie et d'Audioprothèse{% endblock %}</title>
</head>
 
<body>
{% block dachbord %}
            {% include 'Layout/partials/dachbord.html.twig' %}
        {% endblock %}
    <header>
        <!-- Barre de navigation ou menu -->
         
    </header>
    <main>
  

        {% block body %}{% endblock %}
    </main>
    <script src="assets/js/main.js"></script>

    <!-- ====== ionicons ======= -->
    <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>
    <script src="{{ asset('assets/admin/js/main.js') }}"></script>
     <script>
    function filterCustomers() {
        let input = document.getElementById("searchInput").value.toLowerCase();
        let rows = document.querySelectorAll(".customerRow");

        rows.forEach(row => {
            let cin = row.cells[0].textContent.toLowerCase();
            let nom = row.cells[1].textContent.toLowerCase();

            if (cin.includes(input) || nom.includes(input)) {
                row.style.display = ""; // Affiche la ligne
            } else {
                row.style.display = "none"; // Cache la ligne
            }
        });
    }
</script>
</body>
</html>
